---
phase: 01-shared-infrastructure-repo-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/model-catalog.md
  - shared/skill-template.md
autonomous: true
requirements: [INFRA-01, INFRA-02, STRUCT-04]
must_haves:
  truths:
    - "A shared/ directory exists at repo root, sibling to skills/, with no SKILL.md (it is not a skill directory)"
    - "shared/model-catalog.md lists every Higgsfield image and video model from PROJECT.md with params, aspect ratios, durations, and media roles"
    - "shared/model-catalog.md carries a 'last verified: 2026-05-24 via models_explore' annotation and a directive to re-query on parameter failure"
    - "shared/skill-template.md documents the frontmatter standard (name <=64 lowercase-hyphens unique across EN+FR; description <=1024 third-person with 'Use when' triggers)"
  artifacts:
    - path: "shared/model-catalog.md"
      provides: "Canonical ~38-model catalog (image + video) — source of truth for all per-skill routing tables"
      contains: "seedance_2_0"
      min_lines: 80
    - path: "shared/skill-template.md"
      provides: "STRUCT-04 frontmatter standard + lean SKILL.md template/section order"
      contains: "Use when"
      min_lines: 40
  key_links:
    - from: "shared/model-catalog.md"
      to: "PROJECT.md captured catalog"
      via: "verbatim model IDs and constraints (no invented values)"
      pattern: "verified: models_explore 2026-05-24"
---

<objective>
Create the `shared/` directory at repo root and populate it with the two foundational
references that everything downstream depends on: the canonical Higgsfield model catalog
(INFRA-02) and the frontmatter/SKILL.md standard doc (STRUCT-04). Creating `shared/`
itself satisfies INFRA-01.

Purpose: `shared/model-catalog.md` is the ground truth for every skill's model routing
table and per-skill model-specs.md in Phases 2-3. The frontmatter standard must exist
before any skill frontmatter is rewritten. No skill content is written here.

Output: `shared/model-catalog.md`, `shared/skill-template.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-shared-infrastructure-repo-hygiene/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md

<interfaces>
<!-- Source-of-truth model data lives in PROJECT.md "Context" section (captured 2026-05-24).
     Use ONLY these IDs/categories. Do NOT invent resolutions, durations, fps, or formats. -->

Video models (~18): seedance_2_0, seedance_1_5, minimax_hailuo, wan2_6, wan2_7, kling2_6,
  kling3_0, grok_video, veo3, veo3_1, veo3_1_lite, cinematic_studio_3_0,
  cinematic_studio_video (v1/v2), marketing_studio_video, higgsfield_preset

Image models (~20): nano_banana / _2 / _pro, soul_2 / soul_cinematic / soul_cast /
  soul_location, seedream_v4_5 / v5_lite, z_image, flux_2 / flux_kontext, kling_omni_image,
  gpt_image / gpt_image_2, grok_image, cinematic_studio_2_5, marketing_studio_image,
  ms_image (DTC Ads), image_auto

Per-model fields each model declares (from models_explore): parameters (resolution/quality/
  mode/genre), aspect_ratios, durations OR duration_range, media roles (image, start_image,
  end_image, video, audio).

Known concrete spec (from existing repo README — the ONLY confirmed seedance_2_0 numbers):
  seedance_2_0 output 4-15s, 720p, with sound; input limits image <=9 (<30MB ea),
  video <=3 (<50MB ea, total 2-15s), audio <=3 (<15MB ea, total <=15s), combined <=12 files.
  Treat any other per-model number as "[from models_explore]" placeholder, NOT invented.

Frontmatter spec (STACK.md section 1, verified 2026-05-24):
  name: <=64 chars, lowercase letters/numbers/hyphens only, no XML tags, "anthropic"/"claude"
    forbidden, must match directory name, unique across EN + FR variants.
  description: <=1024 chars, non-empty, third person, what-it-does + when-to-use; combined
    description + when_to_use truncated to 1536 chars in listing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared/ and write shared/model-catalog.md</name>
  <files>shared/model-catalog.md</files>
  <action>
    Create the `shared/` directory at repo root (sibling to `skills/`) by writing the
    catalog file into it — this satisfies INFRA-01 (per D-01, shared/ is NOT a skill
    directory: do NOT add a SKILL.md to it). Write `shared/model-catalog.md` (INFRA-02,
    D-02) documenting the full Higgsfield catalog from PROJECT.md's Context section.

    Structure: (1) a header line reading
    `_Last verified: 2026-05-24 via models_explore. If a parameter is rejected at generation
    time, call higgsfield:models_explore to refresh this model's schema._`;
    (2) a table of contents (file exceeds 100 lines); (3) a VIDEO MODELS section and an
    IMAGE MODELS section, each listing every model ID from the interfaces block, grouped by
    family where natural.

    For each model record: model ID, one-line purpose, and the four constraint fields
    (parameters, aspect_ratios, durations/duration_range, media roles). Use the concrete
    seedance_2_0 numbers from the interfaces block. For every other model where PROJECT.md
    does not give exact numbers, write `[from models_explore — re-verify]` for that field
    instead of inventing a value (per D-02, never fabricate specs — this is the canonical
    "4K / .webm" failure mode). Annotate the seedance_2_0 spec block with
    `<!-- verified: models_explore 2026-05-24 -->`.

    Include a short "How skills use this catalog" note: skills filter this to the 2-4 models
    relevant to their style in their own references/model-specs.md (Phases 2-3); this file is
    the single source of truth and must not be duplicated inline in any SKILL.md. Do NOT add
    per-skill routing decisions here (those belong to Phases 2-3).
  </action>
  <verify>
    <automated>cd "$REPO_ROOT" && test -d shared && ! test -f shared/SKILL.md && test -f shared/model-catalog.md && grep -q "verified: models_explore 2026-05-24" shared/model-catalog.md && for m in seedance_2_0 wan2_6 wan2_7 veo3_1 cinematic_studio_3_0 marketing_studio_video kling3_0 nano_banana soul_cinematic ms_image gpt_image flux_2; do grep -q "$m" shared/model-catalog.md || { echo "MISSING MODEL: $m"; exit 1; }; done && ! grep -Eiq '3840[x×]2160|\.webm|[^0-9]8K[^0-9]' shared/model-catalog.md && echo OK</automated>
  </verify>
  <done>shared/ exists with no SKILL.md; model-catalog.md contains all listed video + image model IDs, the verification annotation, the re-verify directive, and zero invented "4K/8K/.webm/3840x2160" specs.</done>
</task>

<task type="auto">
  <name>Task 2: Write shared/skill-template.md (STRUCT-04 frontmatter standard)</name>
  <files>shared/skill-template.md</files>
  <action>
    Write `shared/skill-template.md` documenting the STRUCT-04 standard (per D-08: this
    phase defines the STANDARD only — no per-skill frontmatter is rewritten here).

    Include: (1) the frontmatter rules table from the interfaces block — `name` <=64 chars
    lowercase-letters-numbers-hyphens-only, must match directory name, "anthropic"/"claude"
    forbidden, and MUST be unique across EN and FR variants (FR variants suffix `-fr`, e.g.
    `higgsfield-cinematic` / `higgsfield-cinematic-fr`); `description` <=1024 chars, third
    person, includes what-it-does AND an explicit "Use when…" trigger clause, key triggers
    in the first ~100 chars; note the combined description + when_to_use 1536-char listing
    cap and that overflow triggers go in `when_to_use`.
    (2) Two short worked examples of a compliant frontmatter block (one EN, one matching FR
    with `-fr` name) showing third-person voice and a "Use when…" clause.
    (3) A skeletal lean SKILL.md section order (What this skill does → Model routing → Prompt
    workflow → Generation workflow pointer to ../../shared/generation-flow.md → Reference
    materials), noting the 500-line hard cap and one-level-deep references rule.

    Naming guidance: new skill names drop the `seedance-` prefix in favor of `higgsfield-<style>`
    (current `seedance-*` names are valid format but misleading for the multi-model rework);
    actual renaming happens in Phases 2-3, not here. Keep this file concise (a standard, not
    an encyclopedia).
  </action>
  <verify>
    <automated>cd "$REPO_ROOT" && test -f shared/skill-template.md && grep -q "Use when" shared/skill-template.md && grep -Eq "64" shared/skill-template.md && grep -Eq "1024" shared/skill-template.md && grep -q -- "-fr" shared/skill-template.md && grep -qi "third person" shared/skill-template.md && echo OK</automated>
  </verify>
  <done>shared/skill-template.md documents the name (<=64, lowercase-hyphens, unique across EN+FR) and description (<=1024, third-person, "Use when…") rules with EN + FR `-fr` examples and the lean SKILL.md section order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | This plan writes static markdown documentation only — no untrusted input, no code execution, no network calls, no package installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information disclosure | shared/model-catalog.md spec accuracy | mitigate | Source all values from PROJECT.md's captured catalog; mark unknowns `[from models_explore — re-verify]`; never fabricate — fabricated specs cause wrong prompts and wasted credits downstream. |
| T-01-02 | Tampering | Documentation drift over time | accept | Catalog carries a "last verified 2026-05-24" date + re-query directive; Phase 5 runs the formal accuracy pass. Low risk for a docs-only artifact. |
</threat_model>

<verification>
- `shared/` exists at repo root, is a sibling of `skills/`, and contains NO SKILL.md.
- `shared/model-catalog.md` lists all video + image models from PROJECT.md with the four
  constraint fields each, the 2026-05-24 verification annotation, and the re-query directive.
- No invented specs (no "4K", "8K", "3840x2160", ".webm").
- `shared/skill-template.md` documents the STRUCT-04 frontmatter standard with EN + FR examples.
</verification>

<success_criteria>
- INFRA-01 satisfied: `shared/` directory exists at repo root.
- INFRA-02 satisfied: `shared/model-catalog.md` documents the full catalog, sourced from the
  captured `models_explore` data, with a "last verified" date.
- STRUCT-04 standard documented in `shared/skill-template.md` (per-skill application deferred
  to Phases 2-3 per D-08).
</success_criteria>

<output>
Create `.planning/phases/01-shared-infrastructure-repo-hygiene/01-01-SUMMARY.md` when done.
</output>
