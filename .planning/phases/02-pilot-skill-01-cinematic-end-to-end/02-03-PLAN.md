---
phase: 02-pilot-skill-01-cinematic-end-to-end
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - skills/01-cinematic/fr/SKILL.md
autonomous: false
requirements: [STRUCT-03, MODEL-01, MODEL-05, GEN-01, GEN-05]
must_haves:
  truths:
    - "A French-speaking user gets a French SKILL.md that mirrors the EN structure and routes to the same models"
    - "The FR variant has a unique name (higgsfield-cinematic-fr) so it does not collide with the EN skill in the registry"
    - "The FR variant links to the SAME EN references/ files and the shared generation flow"
    - "Claude can navigate the pilot end-to-end: SKILL.md → model-specs.md → build prompt → confirmation panel → generate only on YES → job report"
  artifacts:
    - path: "skills/01-cinematic/fr/SKILL.md"
      provides: "French mirror of the EN cinematic entry point"
      contains: "higgsfield-cinematic-fr"
      max_lines: 500
  key_links:
    - from: "skills/01-cinematic/fr/SKILL.md"
      to: "../references/model-specs.md"
      via: "markdown relative link (EN references, from fr/ subdir)"
      pattern: "\\.\\./references/model-specs\\.md"
    - from: "skills/01-cinematic/fr/SKILL.md"
      to: "../../../shared/generation-flow.md"
      via: "markdown relative link from fr/ subdir"
      pattern: "shared/generation-flow\\.md"
---

<objective>
Create the French variant `skills/01-cinematic/fr/SKILL.md` mirroring the finalized EN SKILL.md
(Plan 02-02), then verify the whole pilot navigates end-to-end as a working skill, and record the
replicable pattern (STRUCT-03) that Phase 3 copies across the other 14 skills.

Purpose: Proves the FR-variant pattern (separate file, `-fr` suffixed name, links to EN references)
and closes ROADMAP Phase 2 success criteria #3 and #4. The FR task runs in Wave 3 strictly after the
EN SKILL.md is finalized (honors I18N-03 intent).
Output: `skills/01-cinematic/fr/SKILL.md` + a human-verified end-to-end navigation pass.

NOTE (flagged discrepancy — see 02-CONTEXT.md): ROADMAP Phase 2 SC #3 mandates the pilot FR variant
here, while REQUIREMENTS assigns I18N-02/I18N-03 to Phase 4. This plan builds ONLY the single pilot
FR variant as the proven pattern; the bulk FR variants (skills 02–15) and the final FR quality gate
remain Phase 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-pilot-skill-01-cinematic-end-to-end/02-CONTEXT.md

# The finalized EN entry point to mirror (built in Plan 02-02):
@skills/01-cinematic/SKILL.md

# Frontmatter rules + FR Example B (name suffix, third-person "À utiliser quand…" clause):
@shared/skill-template.md

<interfaces>
<!-- FR frontmatter target (per shared/skill-template.md Example B and ROADMAP Phase 2 SC #3): -->
name: higgsfield-cinematic-fr   (unique from EN; -fr suffix; directory skills/01-cinematic/fr/)

<!-- Relative link recalculation from the fr/ subdirectory (one level deeper than the EN SKILL.md):
     EN SKILL.md (skills/01-cinematic/SKILL.md) used  references/...  and ../../shared/...
     FR SKILL.md (skills/01-cinematic/fr/SKILL.md) must use:
       ../references/model-specs.md   (and ../references/camera.md, hooks.md, examples.md)
       ../../../shared/generation-flow.md   and   ../../../shared/mcp-tools.md
     References stay EN-only (D-11) — the FR file links to the EN reference files, it does not
     translate them. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author skills/01-cinematic/fr/SKILL.md mirroring the EN structure (STRUCT-03, MODEL-01/05, GEN-01/05)</name>
  <files>skills/01-cinematic/fr/SKILL.md</files>
  <action>
Create `skills/01-cinematic/fr/SKILL.md` as a French mirror of the finalized EN SKILL.md. Same
section order, same model routing facts, same generation-flow gating — translated body prose into
French. Do NOT re-architect; mirror the EN file section-for-section.

FRONTMATTER: `name: higgsfield-cinematic-fr` (unique, `-fr` suffix per D-09); French third-person
`description` (≤1024 chars) with an explicit "À utiliser quand…" trigger clause (mirror EN Example B
in shared/skill-template.md); `when_to_use` with French overflow triggers; identical `allowed-tools`
block (the mcp__higgsfield__ tool names are NOT translated — same 9 tools as EN).

BODY (French): "Ce que fait cette compétence" / model routing table (same primary cinematic_studio_3_0
+ fallback veo3_1, same per-platform aspect/duration guidance, same MODEL-06 re-verify directive) /
still-image path to cinematic_studio_2_5 (MODEL-05) / prompt-building workflow / opt-in generation
pointer / reference materials.

LINK PATHS (critical — recompute for the fr/ subdir per the `<interfaces>` block):
- generation flow → `../../../shared/generation-flow.md`; tool signatures → `../../../shared/mcp-tools.md`
- references → `../references/camera.md`, `../references/hooks.md`, `../references/examples.md`,
  `../references/model-specs.md` (EN references, NOT translated — D-11)

Keep MCP tool names and model IDs untranslated. Use `higgsfield:tool_name` colon form in prose.
File ≤500 lines (target ≤300).
  </action>
  <verify>
    <automated>cd "/Users/dardan/Desktop/pixelab/Repo/Higgsfield seedance 2.0" && test -f skills/01-cinematic/fr/SKILL.md && grep -q '^name: higgsfield-cinematic-fr$' skills/01-cinematic/fr/SKILL.md && L=$(wc -l < skills/01-cinematic/fr/SKILL.md) && [ "$L" -le 500 ] && grep -q '\.\./references/model-specs\.md' skills/01-cinematic/fr/SKILL.md && grep -q '\.\./\.\./\.\./shared/generation-flow\.md' skills/01-cinematic/fr/SKILL.md && grep -q 'cinematic_studio_3_0' skills/01-cinematic/fr/SKILL.md && grep -q 'mcp__higgsfield__generate_video' skills/01-cinematic/fr/SKILL.md && ! grep -iEq '720p|\.webm|3840|2160' skills/01-cinematic/fr/SKILL.md && echo "PASS lines=$L"</automated>
  </verify>
  <done>fr/SKILL.md exists with name: higgsfield-cinematic-fr, French body mirroring the EN structure, correct fr/-relative links to the EN references/ and ../../../shared/generation-flow.md, same model routing + allowed-tools, ≤500 lines, no invented specs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The 01-cinematic pilot is fully rearchitected: lean EN SKILL.md (≤300/500 lines) + four
references/ files (camera, hooks, examples, model-specs) + French fr/SKILL.md. All links use
correct relative paths; specs are sourced only from the verified models_explore capture.
  </what-built>
  <how-to-verify>
Validate ROADMAP Phase 2 Success Criterion #4 (end-to-end navigation) by walking the pilot as a user:

1. Open `skills/01-cinematic/SKILL.md`. Confirm: frontmatter loads, routing table names
   cinematic_studio_3_0 (primary) and veo3_1 (fallback), and you can follow the link to
   `references/model-specs.md`.
2. In `references/model-specs.md`, confirm the cinematic_studio_3_0 specs are the verified ones:
   aspect ratios 16:9/9:16/1:1, duration 4–15s, NO tunable params; and veo3_1 shows 16:9/9:16 only
   with discrete [4,6,8] durations.
3. Ask Claude (in a fresh session) to "build a cinematic film-noir clip prompt for TikTok". Confirm
   Claude: routes to cinematic_studio_3_0, picks 9:16 + a duration in 4–15s, builds a prompt using
   the camera/hooks references, and presents the prompt for review.
4. Say "generate it". Confirm Claude shows a CONFIRMATION PANEL (model + params + credit cost via
   balance / show_plans_and_credits) and asks for explicit YES — and does NOT call
   higgsfield:generate_video before you answer. (Optional: answer YES only if you want to spend
   credits; otherwise verify the gate held and answer no.)
5. Confirm the French path: open `skills/01-cinematic/fr/SKILL.md`, confirm name is
   higgsfield-cinematic-fr, the body is in French, and its links resolve to the same EN
   references/ files and the shared generation flow.

If all five hold, the pilot template is proven and ready to replicate in Phase 3.
  </how-to-verify>
  <resume-signal>Type "approved" to finalize Phase 2, or describe what failed (broken link, wrong spec, missing confirmation gate) so it can be fixed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FR SKILL.md relative links → reference/shared files | Wrong relative depth from fr/ subdir = broken links = Claude silently loses content |
| FR skill name → Claude skill registry | Name collision with EN variant breaks invocation of one or both |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Spoofing (name collision) | fr/SKILL.md frontmatter name | mitigate | name: higgsfield-cinematic-fr is unique from EN higgsfield-cinematic (D-09); verified by grep |
| T-02-07 | Information disclosure (broken link) | fr/ relative paths | mitigate | Link paths recomputed for the fr/ subdir (../references, ../../../shared); verified by grep and the end-to-end human checkpoint step 5 |
| T-02-08 | Elevation of privilege | generation from FR skill | mitigate | FR variant carries the same opt-in pointer to shared/generation-flow.md; human checkpoint step 4 confirms the confirmation gate holds before any generate call |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs — markdown authoring only |
</threat_model>

<verification>
- fr/SKILL.md exists, name: higgsfield-cinematic-fr, French body, ≤500 lines (ROADMAP Phase 2 SC #3).
- FR links resolve to EN references/ (../references/...) and ../../../shared/generation-flow.md (SC #3).
- End-to-end navigation human-verified: SKILL.md → model-specs.md → prompt → confirmation panel → generate-on-YES only → job report (SC #4).
- The SUMMARY records the replicable decomposition pattern for Phase 3 (STRUCT-03) and the install-time NN-prefix directory mapping (D-09).
</verification>

<success_criteria>
- ROADMAP Phase 2 SC #3 (FR variant) and SC #4 (end-to-end navigation) both satisfied.
- The pilot pattern is documented in the SUMMARY so Phase 3 can copy it without re-deriving it (STRUCT-03).
</success_criteria>

<output>
Create `.planning/phases/02-pilot-skill-01-cinematic-end-to-end/02-03-SUMMARY.md` when done.
The SUMMARY MUST include a "Replicable Pattern" section (the exact SKILL.md + references/ + fr/SKILL.md
decomposition Phase 3 copies) and note the install-time `NN-` prefix → `name` directory mapping (D-09).
</output>
