---
phase: 02-pilot-skill-01-cinematic-end-to-end
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - skills/01-cinematic/SKILL.md
autonomous: true
requirements: [STRUCT-01, STRUCT-02, MODEL-01, MODEL-02, MODEL-03, MODEL-04, MODEL-05, MODEL-06, GEN-01, GEN-02, GEN-03, GEN-05, SPEC-01]
must_haves:
  truths:
    - "A reader opens SKILL.md and within ≤300 lines learns when to use the skill, which model to route to, how to generate, and where the deep content lives"
    - "The model routing table names cinematic_studio_3_0 primary and veo3_1 fallback with rationale and per-platform aspect/duration guidance"
    - "The generation section points to shared/generation-flow.md (confirmation gate) and never auto-generates"
    - "allowed-tools pre-approves exactly the mcp__higgsfield__* tools the skill needs"
  artifacts:
    - path: "skills/01-cinematic/SKILL.md"
      provides: "Lean entry point: frontmatter + routing + generation pointer + references links"
      contains: "higgsfield-cinematic"
      max_lines: 500
  key_links:
    - from: "skills/01-cinematic/SKILL.md"
      to: "../../shared/generation-flow.md"
      via: "markdown relative link"
      pattern: "\\.\\./\\.\\./shared/generation-flow\\.md"
    - from: "skills/01-cinematic/SKILL.md"
      to: "references/model-specs.md"
      via: "markdown relative link"
      pattern: "references/model-specs\\.md"
---

<objective>
Rebuild `skills/01-cinematic/SKILL.md` as the lean (≤300 lines, hard cap 500) reference-implementation
entry point. It replaces the 1329-line monolith with a navigation index: frontmatter, what-it-does,
model routing table, prompt-building workflow, opt-in generation pointer, and links to the four
references/ files created in Plan 02-01.

Purpose: Proves the lean SKILL.md template (STRUCT-01/02) and the model-aware opt-in generation wiring
(MODEL-01..06, GEN-01/02/03/05) on the pilot. This is the file Phase 3 copies 14 times.
Output: `skills/01-cinematic/SKILL.md` (overwritten).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pilot-skill-01-cinematic-end-to-end/02-CONTEXT.md

# The standard this SKILL.md MUST follow (section order, frontmatter rules, line cap, ToC rules):
@shared/skill-template.md

# What the generation section points to (do NOT duplicate — link only):
@shared/generation-flow.md
@shared/mcp-tools.md

# The reference files this SKILL.md links to (built in Plan 02-01):
@skills/01-cinematic/references/model-specs.md

<interfaces>
<!-- Frontmatter target (per shared/skill-template.md Example A and ROADMAP Phase 2 SC #3): -->
name: higgsfield-cinematic   (lowercase-hyphens, ≤64 chars, third-person description with "Use when…")

<!-- allowed-tools the cinematic skill needs (GEN-05). 01-cinematic is NOT a mandatory-I2V skill,
     but cinematic_studio_3_0/veo3_1 accept an optional start_image, so the media pair is included
     for the optional-reference case. presets_show is NOT needed (cinematic_studio_3_0 has no preset
     dependency); select_workspace is NOT needed. Use the double-underscore form ONLY in frontmatter. -->
allowed-tools (mcp__higgsfield__ form): generate_video, generate_image, models_explore,
  job_status, job_display, media_upload, media_confirm, balance, show_plans_and_credits

<!-- Verified routing facts to put in the routing table (full detail in 02-CONTEXT.md / model-specs.md):
     primary cinematic_studio_3_0: 16:9/9:16/1:1, 4–15s, no tunable params, T2V+I2V
     fallback veo3_1: 16:9/9:16 only, [4,6,8] discrete, quality+model params, native audio
     still-image path cinematic_studio_2_5 (MODEL-05): resolution 1k/2k/4k, production still -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the lean SKILL.md frontmatter + model routing + still-image path (STRUCT-01, MODEL-01..05, GEN-05, SPEC-01)</name>
  <files>skills/01-cinematic/SKILL.md</files>
  <action>
Overwrite `skills/01-cinematic/SKILL.md` (currently the 1329-line monolith) following the section
order in `shared/skill-template.md`. Build the frontmatter and the model-routing half of the body:

FRONTMATTER (per D-08/D-09): `name: higgsfield-cinematic`; third-person `description` (≤1024 chars)
opening with the primary trigger and an explicit "Use when…" clause (preserve the strong trigger
keywords from the old description — cinematic, film look, dramatic lighting, depth of field, lens
flare, anamorphic, noir, Steadicam, dolly, crane — but make it third-person and drop the
Seedance-specific framing); overflow triggers in `when_to_use`; `allowed-tools` using the
`mcp__higgsfield__` double-underscore form for exactly: generate_video, generate_image,
models_explore, job_status, job_display, media_upload, media_confirm, balance,
show_plans_and_credits (GEN-05 — no presets_show, no select_workspace).

BODY (first half):
- `## What this skill does` — 2–3 sentences: crafts film-grade cinematic video prompts and routes
  to the best Higgsfield model; outputs a production-ready prompt and, on explicit confirmation,
  generates the asset.
- `## Model routing` — a table with primary `cinematic_studio_3_0` and fallback `veo3_1`, each with
  a one-line rationale (MODEL-01): studio model = dedicated cinematic pipeline; veo3_1 = photorealism
  + native audio fallback. Then a per-platform aspect-ratio + duration recommendation row set
  (MODEL-03/04): 9:16 for TikTok/Reels/Shorts, 16:9 for YouTube/LinkedIn, 1:1 for Instagram feed
  (note 1:1 is cinematic_studio_3_0 only — veo3_1 has no 1:1). State cinematic_studio_3_0 has NO
  tunable params and veo3_1 exposes quality {basic,high,ultra}/model {veo-3-1-preview,veo-3-1-fast}
  (MODEL-02). Include a one-line MODEL-06 directive: call `higgsfield:models_explore` to re-verify
  constraints if a parameter is rejected. Link full tables to [references/model-specs.md](references/model-specs.md).
- `## Still-image path` (MODEL-05) — one short paragraph: for a production still / mood-board frame,
  route to `cinematic_studio_2_5` (resolution 1k/2k/4k) via `higgsfield:generate_image`; link to
  references/model-specs.md.

SPEC-01: no bare "720p" video claim, no 4K/.webm/fps as video claims. Use `higgsfield:tool_name`
(colon form) in all prose (D-03).
  </action>
  <verify>
    <automated>cd "/Users/dardan/Desktop/pixelab/Repo/Higgsfield seedance 2.0" && head -1 skills/01-cinematic/SKILL.md | grep -q '^---$' && grep -q '^name: higgsfield-cinematic$' skills/01-cinematic/SKILL.md && grep -q 'mcp__higgsfield__generate_video' skills/01-cinematic/SKILL.md && grep -q 'cinematic_studio_3_0' skills/01-cinematic/SKILL.md && grep -q 'veo3_1' skills/01-cinematic/SKILL.md && grep -q 'cinematic_studio_2_5' skills/01-cinematic/SKILL.md && ! grep -iEq '720p|\.webm|3840|2160' skills/01-cinematic/SKILL.md && echo PASS</automated>
  </verify>
  <done>SKILL.md opens with valid frontmatter (name: higgsfield-cinematic, allowed-tools with the 9 mcp__higgsfield__ tools), a routing table naming primary/fallback with rationale + per-platform guidance, a still-image path to cinematic_studio_2_5, the models_explore re-verify directive, and no invented video specs.</done>
</task>

<task type="auto">
  <name>Task 2: Add prompt-building workflow, opt-in generation pointer, and references links; verify line cap (STRUCT-01/02, GEN-01/02/03, MODEL-06)</name>
  <files>skills/01-cinematic/SKILL.md</files>
  <action>
Append the second half of the body to `skills/01-cinematic/SKILL.md`:

- `## Prompt-building workflow` — numbered steps: (1) gather intent (style, subject, mood, aspect
  ratio, duration, platform); (2) select model per the routing table above; (3) build the prompt
  using the craft principles in references/ (link camera.md, hooks.md, examples.md); (4) present the
  draft prompt + assembled params to the user for review BEFORE any generation.
- `## Opt-in generation` — a brief note (do NOT duplicate the flow): generation costs Higgsfield
  credits and requires explicit user confirmation. Link the canonical flow at
  [`../../shared/generation-flow.md`](../../shared/generation-flow.md) (GEN-01/02/03 — confirmation
  gate, balance/show_plans_and_credits cost surface, generate_video → poll job_status → job_display).
  State this skill's model (cinematic_studio_3_0) and that media upload is OPTIONAL here (only when
  the user supplies a start_image/end_image reference) — 01-cinematic is not a mandatory-I2V skill.
  Reference tool signatures at [`../../shared/mcp-tools.md`](../../shared/mcp-tools.md).
- `## Reference materials` — relative links to all four references files:
  references/camera.md, references/hooks.md, references/examples.md, references/model-specs.md
  (each with a one-line description). One level deep only; do not chain.

After writing, confirm the whole file is ≤500 lines (target ≤300). If over 300, move any remaining
encyclopedic prose into the appropriate references/ file rather than trimming meaning (do NOT reduce
scope — relocate content per D-10/D-12).
  </action>
  <verify>
    <automated>cd "/Users/dardan/Desktop/pixelab/Repo/Higgsfield seedance 2.0" && L=$(wc -l < skills/01-cinematic/SKILL.md) && [ "$L" -le 500 ] && grep -q '\.\./\.\./shared/generation-flow\.md' skills/01-cinematic/SKILL.md && grep -q 'references/camera\.md' skills/01-cinematic/SKILL.md && grep -q 'references/hooks\.md' skills/01-cinematic/SKILL.md && grep -q 'references/examples\.md' skills/01-cinematic/SKILL.md && grep -q 'references/model-specs\.md' skills/01-cinematic/SKILL.md && grep -qi 'confirm' skills/01-cinematic/SKILL.md && echo "PASS lines=$L"</automated>
  </verify>
  <done>SKILL.md is ≤500 lines (target ≤300), links to ../../shared/generation-flow.md and all four references/ files, documents the prompt-building workflow and the opt-in/confirmation-gated generation pointer, and states the optional-media case for this non-I2V skill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md instructions → Claude runtime → Higgsfield MCP | The skill instructs Claude when to call credit-spending generate tools; missing confirmation gate = unauthorized credit spend |
| allowed-tools frontmatter → MCP tool surface | Over-broad allowed-tools pre-approves tools the skill should not silently invoke |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Elevation of privilege | generate_video/generate_image | mitigate | SKILL.md never inlines a generate call; it points to shared/generation-flow.md whose Step 3 is a hard confirmation gate (GEN-01); no generate tool is described as auto-run |
| T-02-04 | Spoofing/over-grant | allowed-tools frontmatter | mitigate | allowed-tools lists exactly the 9 tools the skill needs (GEN-05); presets_show and select_workspace deliberately excluded; verified by grep that no unlisted tool appears in prose |
| T-02-05 | Information disclosure | model params in routing table | mitigate | Routing table states only verified params (no 1:1 for veo3_1, no tunable params for cinematic_studio_3_0); MODEL-06 directive to re-query on rejection |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs — markdown authoring only |
</threat_model>

<verification>
- SKILL.md ≤500 lines (target ≤300) with working YAML frontmatter (ROADMAP Phase 2 SC #1).
- Routing table: cinematic_studio_3_0 primary / veo3_1 fallback (SC #1).
- Generation workflow pointer resolves to ../../shared/generation-flow.md (SC #1, GEN-01/02/03).
- Links to all four reference files present (SC #1).
- allowed-tools pre-approves exactly the needed mcp__higgsfield__* set; no tool used outside it (SC #5, GEN-05).
- No invented specs (SPEC-01): no 720p/4K/.webm/fps video claims.
</verification>

<success_criteria>
- A different Claude instance can read SKILL.md and know: when to activate, which model, how to generate (via shared flow), and where the deep content is — without reading the old monolith.
- Every Phase 2 requirement touched here (STRUCT-01/02, MODEL-01..06, GEN-01/02/03/05, SPEC-01) is observable in the file.
</success_criteria>

<output>
Create `.planning/phases/02-pilot-skill-01-cinematic-end-to-end/02-02-SUMMARY.md` when done.
</output>
